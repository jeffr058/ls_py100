# The following code causes an infinite loop (a loop that never stops iterating). Why?

# counter = 0

# while counter < 5:
#     print(counter)

It will be an infinite loop because without anything causing the counter to change, it will always be 0 and < 5, so the loop will never stop running.