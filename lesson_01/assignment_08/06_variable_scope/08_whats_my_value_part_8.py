# What will the following code do and why? Don't run it until you have tried to answer.

print(greeting)

greeting = 'Hello world!'

# My solution
It will raise an error because in the global scope, the print function is called before the variable is initialized.