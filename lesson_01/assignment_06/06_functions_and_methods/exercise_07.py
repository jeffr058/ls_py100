# Without running the following code, what do you think it will do?

# def foo(bar, qux):
#     print(bar)
#     print(qux)

# foo('Hello')

I think it will result in an error because the interpreter expects foo() to receive two arguments.