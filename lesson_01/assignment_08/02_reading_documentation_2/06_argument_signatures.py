# How many arguments does the str.join method expect? What happens if you call it with fewer or more arguments?

It expects one argument of an iterable. An error will be raised if fewer or more arguments are passed into it.